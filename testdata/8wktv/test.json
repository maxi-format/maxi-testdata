{
    "id": "8wktv",
    "title": "mime:image/* wildcard constraint: matching MIME type passes",
    "description": "A bytes field with mime:image/* constraint must accept a value whose declared MIME type matches the image/* wildcard. The schema constraint is parsed and stored correctly.",
    "category": "valid",
    "tags": [
        "inline-schema",
        "bytes",
        "constraints",
        "annotations"
    ]
}
