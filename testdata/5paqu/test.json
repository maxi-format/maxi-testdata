{
    "id": "5paqu",
    "title": "Explicit (id) field duplicate triggers E016, not the id field",
    "description": "Two OrderItem records with distinct 'id' values but the same 'order_item_id' value (the explicit identifier) must trigger DuplicateIdentifierError E016 in strict mode.",
    "category": "error",
    "mode": "strict",
    "tags": [
        "inline-schema",
        "constraints",
        "references",
        "duplicate-id",
        "error-code",
        "strict"
    ]
}
