{
    "id": "ssh5y",
    "title": "Data-Only Input (No Schema Section)",
    "description": "Input with no schema section and no ### separator is treated as records-only; unknown alias warnings are emitted with default parser settings but parsing succeeds.",
    "category": "valid",
    "tags": [
        "no-schema"
    ]
}
