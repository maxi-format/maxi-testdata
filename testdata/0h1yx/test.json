{
    "id": "0h1yx",
    "title": "Default Values Applied to Omitted Trailing Fields",
    "description": "When a record omits trailing fields that have defaults, the parser fills them in with the declared default values.",
    "category": "valid",
    "mode": "lax",
    "tags": [
        "inline-schema",
        "defaults"
    ]
}
