{
    "id": "jmnjv",
    "title": "Value not in quoted enum list triggers E008",
    "description": "A data record providing a value that is not a member of the enum (even when the enum uses quoted multi-word values) must trigger ConstraintViolationError E008.",
    "category": "error",
    "tags": [
        "inline-schema",
        "enum",
        "constraints",
        "error-code"
    ],
    "parserOptions": {
        "allowConstraintViolations": "error"
    }
}
