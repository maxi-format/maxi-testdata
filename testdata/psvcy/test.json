{
    "id": "psvcy",
    "title": "Omitted trailing fields are filled with null by default",
    "description": "When a record omits trailing fields, the parser fills them with null by default (allowMissingFields defaults to 'null'). No warnings or errors are emitted.",
    "category": "valid",
    "tags": [
        "inline-schema"
    ]
}
