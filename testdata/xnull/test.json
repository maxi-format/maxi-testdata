{
    "id": "xnull",
    "title": "Explicit null (~) on required field with default emits warning (lax)",
    "description": "Using ~ to explicitly null a required field that has a default value should emit a MissingRequiredFieldError warning in lax mode and result in null (not the default).",
    "category": "warning",
    "mode": "lax"
}
