{
    "id": "xnull",
    "title": "Explicit null (~) on required field with default emits warning",
    "description": "Using ~ to explicitly null a required field that has a default value should emit a MissingRequiredFieldError warning with default parser settings and result in null (not the default).",
    "category": "warning",
    "tags": [
        "inline-schema",
        "required",
        "defaults",
        "null-values",
        "warning"
    ]
}
