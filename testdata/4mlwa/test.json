{
    "id": "4mlwa",
    "title": "Invalid enum value with default parser settings emits warning",
    "description": "with default parser settings a value not in the enum list emits a ConstraintViolationError warning; the value is kept as-is.",
    "category": "valid",
    "tags": [
        "inline-schema",
        "enum",
        "warning"
    ]
}
