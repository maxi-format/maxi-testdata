{
    "id": "sfsuv",
    "title": "Duplicate id with default parser settings emits warning",
    "description": "with default parser settings two records with the same id emit a DuplicateIdentifierError warning; both records are still parsed.",
    "category": "valid",
    "tags": [
        "inline-schema",
        "references",
        "duplicate-id",
        "warning"
    ]
}
