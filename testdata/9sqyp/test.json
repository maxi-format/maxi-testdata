{
    "id": "9sqyp",
    "title": "Type name starting with underscore (_Meta) is invalid per §3.3.2",
    "description": "Per §3.3.2, type names must start with a letter [a-zA-Z]. The type name _Meta starts with an underscore, which is invalid. The alias _M is valid per §3.3.3, but the type name itself must be rejected.",
    "category": "error",
    "tags": [
        "inline-schema",
        "error-code"
    ]
}
