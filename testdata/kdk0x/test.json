{
    "id": "kdk0x",
    "title": "Default settings: extra trailing fields silently discarded",
    "description": "with default parser settings, a record that supplies more field values than the schema defines must be accepted. Extra values beyond the schema field count are silently discarded. No error and no warning are required for this case.",
    "category": "valid",
    "tags": [
        "inline-schema"
    ]
}
