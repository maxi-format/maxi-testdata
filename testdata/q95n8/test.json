{
    "id": "q95n8",
    "title": "Unresolved reference with default parser settings emits warning",
    "description": "with default parser settings a reference to a non-existent object emits an UnresolvedReferenceError warning; the record is still parsed.",
    "category": "valid",
    "tags": [
        "inline-schema",
        "references",
        "warning"
    ]
}
